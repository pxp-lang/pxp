<?php

// A file that dumps the type of all scalar values in PHP.
dumpType("Hello, world!"); // string
dumpType('Hello, world!'); // string
dumpType(12345);           // integer
dumpType(123.45);          // float
dumpType(true);            // bool
dumpType(false);           // bool
dumpType(null);            // null