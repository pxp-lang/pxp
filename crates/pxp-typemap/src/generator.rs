use pxp_ast::Statement;

#[derive(Debug, Clone, Default)]
pub struct TypeMapGenerator {
    
}

impl TypeMapGenerator {
    pub fn new() -> Self {
        Self::default()
    }

    pub fn generate(&mut self, ast: &mut [Statement]) {
        
    }
}